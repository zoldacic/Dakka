class AgotCard {
    constructor(title, text, cost, type, unique, expansion, id) {
	    this.title = title;
	    this.text = text;
	    this.cost = cost;
	    this.type = type;
	    this.unique  = unique;
	    this.expansion = expansion;
	    this.id = id;
    }
}

export {AgotCard}